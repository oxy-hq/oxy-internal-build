name: Publish Docker Image

on:
  workflow_dispatch:

jobs:
  build-and-publish:
    concurrency:
      group: docker-publish-arm-${{ github.ref_name }}
      cancel-in-progress: true
    runs-on: ubuntu-24.04-arm
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
      - uses: actions/create-github-app-token@v2
        name: Create GitHub App Token
        id: app-token
        with:
          app-id: ${{ vars.ARGO_APP_ID }}
          private-key: ${{ secrets.ARGO_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: |
            oxy-internal
            oxy-internal-build
      - uses: actions/checkout@v4
        id: checkout
        with:
          repository: oxy-hq/oxy-internal
          token: ${{ steps.app-token.outputs.token }}
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/oxy-internal-build:latest
            ghcr.io/${{ github.repository_owner }}/oxy-internal-build:${{ steps.checkout.outputs.commit }}
